# Public build-time values only. Never put secrets in this app.
# API endpoint used to check invitation validity before redirecting to a store.
WEB_API_URL=
# Optional: leave blank until the corresponding store listing is available.
WEB_IOS_STORE_URL=
WEB_ANDROID_STORE_URL=
